<div id="group">
  <div class='img-div'>
    <%= image_tag group.icon %>
  </div>

  <div>
    <h3><%= group.name %></h3>
    <%= group.created_at.strftime("%d %b %Y") %>
  </div>

  <p>
    $<%= group.entities.sum(:amount) %>
  </p>

</div>
